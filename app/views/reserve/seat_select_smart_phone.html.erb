<div id="room-select" class="cf">
  <div id="room">Room<span class="space">&thinsp;</span></div>
  <div id="btn-a">A<span class="space">&thinsp;</span></div>
  <div id="btn-b">B<span class="space">&thinsp;</span></div>
</div>
<div class="time">
  <p>
    公演時間<br><span><%= @attrs.first.time.strftime("%m/%d %H:%M") %></span>
  </p>
</div>
<div id="seat-select">
  <div id="screen">
    Screen<span class="space">&thinsp;</span>
  </div>
  <% @attrs.each_with_index do |f, i| %>
    <% if i == 0 || i == 5 %>
      <div class="each-room">
    <% end %>
    <div id="seat-<%= i + 1 %>">
      <div class="seat-img">
        <% if f.status == 0%>
            <%= link_to image_tag('seat.png', size: 80), reserve_form_path(f.id) %>
        <% else %>
            <%= image_tag('seat_soldout.png', size: 80) %>
        <% end %>
      </div>
      <div class="seat-label">
        <% case i % 5 + 1 %>
        <% when 1 %>
            S1
        <% when 2 %>
            S2
        <% when 3 %>
            S3
        <% when 4 %>
            A1
        <% else %>
            A2
        <% end %>
      </div>
    </div>
    <% if i == 4 || i == 9 %>
        </div>
    <% end %>
  <% end %>
  <p class="price">
    <span>前売り料金</span><br>
    S<span>席</span>: 300<span>円</span><br>
    A<span>席</span>: 200<span>円</span><br>
    <span>当日料金</span><br>
    S<span>席</span>: 400<span>円</span><br>
    A<span>席</span>: 300<span>円</span><br>
  </p>
</div>